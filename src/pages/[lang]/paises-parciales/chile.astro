---
export function getStaticPaths() {
  return [
    { params: { lang: 'es', pais: 'chile' } },
    { params: { lang: 'pt', pais: 'chile' } },
  ];
}

import { t } from '../../../i18n/utils.ts';

const { lang } = Astro.params;
const locale = lang === 'pt' ? 'pt' : 'es';
---

<div class="p-6 bg-white border-2 border-gray-200 rounded-lg">
  <h2 class="text-2xl font-bold text-cafeoscuro mb-4">{t('pais.chile.titulo', locale)}</h2>
  <p class="text-gray-600">{t('pais.chile.descripcion', locale)}</p>
  <ul class="mt-4 space-y-2">
    <li>• {t('pais.chile.item1', locale)}</li>
    <li>• {t('pais.chile.item2', locale)}</li>
    <li>• {t('pais.chile.item3', locale)}</li>
    <li>• {t('pais.chile.item4', locale)}</li>
  </ul>
</div>
